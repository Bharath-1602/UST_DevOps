apiVersion: v1
kind: Pod
metadata:
  name: emp-demo
spec:
  containers:
  - name: app
    image: nginx
    volumeMounts:
    - mountPath: /cache
      name: scratch-space
  volumes:
    - name: scratch-space
      emptyDir: {}
