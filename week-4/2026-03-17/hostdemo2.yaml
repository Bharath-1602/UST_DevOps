apiVersion: v1
kind: Pod
metadata:
  name: host-2
spec:
  containers:
  - name: app
    image: nginx
    volumeMounts:
    - mountPath: /host-data
      name: host-volume
  volumes:
  - name: host-volume
    hostPath:
      path: /usr/data/app
      type: Directory
